<p id="notice"><%= notice %></p>

 <h1><%= @list.name %></h1>

<table>
	<% @list.items.all.each do |i| %>
		<tr>	
  				<td> <%= i.name %></td>
		</tr>
	<% end %>
</table>


<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
